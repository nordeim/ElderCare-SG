version: "3.8"

services:
  app:
    image: eldersg-daycare-platform-app
    container_name: eldersg-app
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      # Non-secret defaults; override via .env.production or deployment secrets
      APP_ENV: ${APP_ENV:-production}
      APP_DEBUG: ${APP_DEBUG:-false}
      APP_URL: ${APP_URL:-http://localhost}
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      DB_HOST: ${DB_HOST:-mysql}
      DB_PORT: ${DB_PORT:-3306}
      DB_DATABASE: ${DB_DATABASE:-laravel}
      DB_USERNAME: ${DB_USERNAME:-sail}
      DB_PASSWORD: ${DB_PASSWORD:-password}
    volumes:
      - eldersg-app-storage:/var/www/html/storage
      - ./public:/var/www/html/public:ro
    expose:
      - "9000"
    depends_on:
      - mysql
      - redis
    entrypoint: ["/usr/local/bin/entrypoint.sh"]
    command: ["php-fpm"]
    healthcheck:
      test: ["CMD-SHELL", "test -f /tmp/app.ready"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 90s

  nginx:
    image: nginx:1.27-alpine
    container_name: eldersg-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      # Read-only mount of Nginx config and static assets
      - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./public:/var/www/html/public:ro
      # Optional: custom mime/types or extra conf.d snippets
      # - ./docker/mime.types:/etc/nginx/mime.types:ro
    depends_on:
      - app
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

  mysql:
    image: mariadb:10.11
    container_name: eldersg-mysql
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD:-root_password}
      MARIADB_DATABASE: ${DB_DATABASE:-laravel}
      MARIADB_USER: ${DB_USERNAME:-sail}
      MARIADB_PASSWORD: ${DB_PASSWORD:-password}
    ports:
      - "3306:3306"
    volumes:
      - eldersg-mysql-data:/var/lib/mysql
    command: ["mysqld", "--character-set-server=utf8mb4", "--collation-server=utf8mb4_unicode_ci"]
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u${MARIADB_USER:-sail} -p${MARIADB_PASSWORD:-password} || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 60s

  redis:
    image: redis:7.4
    container_name: eldersg-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

# Optional: Mailhog (commented out for production)
#  mailhog:
#    image: mailhog/mailhog:v1.0.1
#    container_name: eldersg-mailhog
#    restart: unless-stopped
#    ports:
#      - "8025:8025" # Web UI
#      - "1025:1025" # SMTP
#    healthcheck:
#      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:8025 || exit 1"]
#      interval: 10s
#      timeout: 5s
#      retries: 12
#      start_period: 30s

volumes:
  eldersg-mysql-data:
  eldersg-app-storage:

